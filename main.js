$(function(){
	var params = {
      root: 'body',
      scroller: '.scroller',
      bar: '.scrollbar',
      barOnCls: 'baron'
    };

    baron(params);

    var params = {
      root: '.container',
      scroller: '.scroller',
      bar: '.scrollbar',
      barOnCls: 'baron'
    };

    baron(params);
});